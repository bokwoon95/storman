[project]
name = "storman"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "camoufox>=0.4.11",
    "playwright>=1.55.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"

[tool.ruff.lint]
extend-select = ["I"]
